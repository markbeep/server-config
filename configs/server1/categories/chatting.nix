{
  permissions = { };

  channels = {
    general = {
      description = "General chatting channel";
      permissions = { };
    };

    off-topic = {
      description = "Off topic stuff should go here";
      permissions = { };
    };
  };
}
